# Auto-load the flake devShell on `cd` (requires direnv + nix-direnv).
# Run `direnv allow` once to trust this file.
# Provides every project tool (just, shellcheck, bats, vulnix, cachix, …) — see
# flake.nix devShells.default. (Needs T17: the devShell must exist.)
use flake
